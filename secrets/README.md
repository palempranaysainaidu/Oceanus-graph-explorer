# Secrets folder

Store downloaded credential files here (e.g. CognoDB instance `.txt` downloads).

This directory is gitignored. Only `README.md` and `.gitignore` are tracked.

Copy values into the project root `.env` (also gitignored) for the FastAPI backend.
